import { NavigationLineDirective } from './navigation-line.directive';

describe('NavigationLineDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigationLineDirective();
    expect(directive).toBeTruthy();
  });
});
